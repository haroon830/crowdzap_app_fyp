import * as React from "react";

/*
    props: type (of which type placeholder needed) > listedProperties
                                                   > keys
 */
//TODO ::: ADD different type of palace holder
export default function PlaceHolder(props){
    return(
        <button className="btn btn-lg btn-green"> Retry</button>
    )
}